class ConnectHub():
	Login login;
	IRedSocial tiktok
	IRedSocial facebook
	IRedSocial instragram;

	init();

class Login():
	UserController UC;
	IFileHandler dataUsers;

	init();
	login();
	createAccount();

class FileHandler:
	String path;

	save();
	load();

class RedSocial(String name, User u):
	String name;
	User user;
	IFileHandler dataPublications;
	IPublications publications;
	PublicationGenerator PG;

	myProfile();
	publish();
	showPublications();

class Publications:
	List<Publication> publications;

	setPublications(List<Publication>);
	getPublications() -> List<Publication>;
	addPublication(Publication p);
	deletePublication(int id);

class PublicationGenerator(User u):
	createVideo() -> Publication p;
	createImage() -> Publication p;
	createText() -> Publication p;
	createVideoAndText() -> Publication p;
	createImageAndText() -> Publication p;

class Publication(User u):
	int id;
	User u;
	Date dateM
	List<Content> content;

class Content:
	String type;
	File f;

class User(String username, String password, Date birthday):
	String username;
	String password;
	Date birthday;
	List<User> followers;
	List<User> followed;

class UserController:
	List<User> users;
	
	setUsers(List<User> );
	getUsers() -> List<User> ;
	addUser(User u);
	deleteUser(String username);
	getUser(String username);